{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make",
            "group": "build",
            "type": "shell",
            "command": "${workspaceFolder}/build.bat",
            "options": {
                "cwd": "${workspaceFolder}",
            }
        },
        {
            "label": "Make Clean",
            "group": "build",
            "type": "shell",
            "command": "${workspaceFolder}/clean.bat",
            "options": {
                "cwd": "${workspaceFolder}",
            }
        }
    ]
}